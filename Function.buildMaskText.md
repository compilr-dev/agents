---
title: "buildMaskText"
parent: Functions
nav_order: 1
---


# Function: buildMaskText()

```ts
function buildMaskText(stamp): string;
```

Defined in: context/observation-masker.ts:388

Build the compact mask text that replaces the original content.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `stamp` | `TurnStamp` |

## Returns

`string`
